<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.somsom.cyworld.post.setting.dao.SettingDAO">
 
 	<!-- 자기소개, 프로필 사진, 오늘의 기분, 인사말 등록 -->
 	<insert id="insertSetting" parameterType="map">
 		INSERT INTO `setting`
 		(
 			targetUserId
 			, `userId`
 			, `introduce`
 			, `motion`
 			, `content`
 			, `imagePath`
 			, `createdAt`
 			, `updatedAt`
 		)
 		VALUES
 		(
 			targetUserId
 			, #{userId}
 			, #{introduce}
 			, #{motion}
 			, #{content}
 			, #{imagePath}
 			, now()
 			, now()
 		)
 	</insert>
 	
 	
 	<!-- 상단 소개말 등록 -->
 	<insert id="insertIntroduceSetting" parameterType="map">
 		INSERT INTO `setting`
 		(
 			targetUserId
 			, `userId`
 			, `introduce`
 			, `createdAt`
 			, `updatedAt`
 		)
 		VALUES
 		(
 			targetUserId
 			, #{userId}
 			, #{introduce}
 			, now()
 			, now()
 		)
 	</insert>
 	
 	<!-- 프로필 이미지 등록 -->
 	<insert id="insertProfileImageSetting" parameterType="map">
 	 	INSERT INTO `setting`
 		(
 			targetUserId
 			, `userId`
 			, `imagePath`
 			, `createdAt`
 			, `updatedAt`
 		)
 		VALUES
 		(
 			targetUserId
 			, #{userId}
 			, #{imagePath}
 			, now()
 			, now()
 		)
 	</insert>
 	
 	<!-- 프로필 컨텐츠 등록 -->
 	<insert id="insertProfileIntroduceSetting" parameterType="map">
 	 	INSERT INTO `setting`
 		(
 			targetUserId
 			, `userId`
 			, `content`
 			, `createdAt`
 			, `updatedAt`
 		)
 		VALUES
 		(
 			targetUserId
 			, #{userId}
 			, #{content}
 			, now()
 			, now()
 		)
 	</insert>
 	
 	
 	
 	
 	
 	
 	<!-- 설정 리스트  -->
 	<select id="selectSetting" parameterType="int" 
 	resultType="com.somsom.cyworld.post.setting.model.Setting">
 		SELECT
 			`id`
 			, targetUserId
 			, `userId`
 			, `introduce`
 			, `motion`
 			, `content`
 			, `imagePath`
 			, `createdAt`
 			, `updatedAt`
 		FROM 
 			`setting`	
 		WHERE 
 			`targetUserId` = #{targetUserId}
 	</select>
 	
 </mapper>